pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "GitHub change detected. Rolling out new build."'
                
            }
        }
    }
}